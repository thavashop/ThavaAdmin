<div class="row page-titles mx-0">
    <div class="col-sm-6 p-md-0">
        <div class="welcome-text">
            <h4>Hi, welcome back!</h4>
            <span class="ml-1">{{adminName}}</span>
        </div>
    </div>
    <div class="col-sm-6 p-md-0 justify-content-sm-end mt-2 mt-sm-0 d-flex">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0)">Accounts</a></li>
            <li class="breadcrumb-item active"><a href="javascript:void(0)">List</a></li>
        </ol>
    </div>
</div>
{{> notifications}}
<div></div>
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">Admin accounts Datatable</h4>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="example" class="display" style="min-width: 845px">
                        <thead>
                            <tr>
                                <th>Username</th>
                                <th>First Name</th>
                                <th>Last Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                            </tr>
                        </thead>
                        <tbody style="color: black;">
                            {{#each accounts}}
                            <tr>
                                <td>{{username}}</td>
                                <td>{{firstName}}</td>
                                <td>{{lastName}}</td>
                                <td>{{email}}</td>
                                <td>{{phone}}</td>
                            </tr>
                            {{/each}}
                        </tbody>
                        <tfoot>
                            {{!-- <ul class="pagination pagination-gutter">
                                <li class="page-item page-indicator">
                                    <a class="page-link" href="/products?page={{dec page}}">
                                        <i class="icon-arrow-left"></i></a>
                                </li>
                                {{#each pages}}
                                <li class="page-item {{#if (equals ../page this)}}active{{/if}}">
                                    <a class="page-link" href="/products?page={{this}}">{{this}}</a></li>
                                {{/each}}
                                <li class="page-item page-indicator">
                                    <a class="page-link" href="/products?page={{inc page}}">
                                        <i class="icon-arrow-right"></i></a>
                                </li>
                            </ul> --}}
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>